		<%= form_for [@book, @book_comment] do |f| %>

      <%= flash[:error] %>

			<div>
				<%= f.text_area :comment, cols:'50', rows:'5' %>
			</div>
			<%= f.submit "Create Comment", class: "btn-sm btn-primary" %>
		<% end %>